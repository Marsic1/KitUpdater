<Window x:Class="KitUpdaterEditor.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:l="clr-namespace:KitUpdater.Shared"
        Title="{l:Loc 'Impostazioni WebDAV'}" Height="420" Width="480"
        WindowStartupLocation="CenterOwner" Background="#1E1E1E" Foreground="#E0E0E0"
        FontFamily="Segoe UI" ResizeMode="NoResize" ShowInTaskbar="False" Loaded="Window_Loaded">
    <Window.Resources>
        <Style x:Key="BaseButton" TargetType="Button">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3E3E42"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="#005A9E"/>
            <Setter Property="BorderBrush" Value="#004A82"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0078D7"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Button" BasedOn="{StaticResource BaseButton}"/>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="Margin" Value="0,8,0,2"/>
        </Style>
        <Style x:Key="FieldBox" TargetType="TextBox">
            <Setter Property="Background" Value="#0D0D0D"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5,3"/>
        </Style>
        <!-- Come FieldBox, ma per PasswordBox (TargetType diverso: uno stile
             TextBox applicato a PasswordBox fa crashare il parser XAML) -->
        <Style x:Key="PasswordFieldBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="#0D0D0D"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5,3"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{l:Loc 'Impostazioni Nextcloud / WebDAV'}" FontSize="18" FontWeight="Bold" Foreground="#F0F0F0" Margin="0,0,0,5"/>
        <TextBlock Grid.Row="0" Text="{l:Loc 'La password viene salvata cifrata (DPAPI) e resta leggibile solo dal tuo utente Windows.'}" FontSize="11" Foreground="#888888" Margin="0,30,0,0"/>

        <StackPanel Grid.Row="1" Margin="0,10,0,10">
            <Label Content="{l:Loc 'Lingua (vuoto = lingua di Windows; valido dopo il riavvio)'}"/>
            <TextBox x:Name="TxtLanguage" Style="{StaticResource FieldBox}"/>
            <Label Content="{l:Loc 'URL del server (es. https://cloud.miosito.it)'}"/>
            <TextBox x:Name="TxtServerUrl" Style="{StaticResource FieldBox}"/>
            <Label Content="{l:Loc 'Nome utente'}"/>
            <TextBox x:Name="TxtUsername" Style="{StaticResource FieldBox}"/>
            <Label Content="{l:Loc &quot;Password dell'app (Nextcloud → Impostazioni → Sicurezza → Password dell'app)&quot;}"/>
            <PasswordBox x:Name="TxtPassword" Style="{StaticResource PasswordFieldBox}"/>
            <Label Content="{l:Loc 'Percorso remoto del manifest (es. /MioKit/manifest.json)'}"/>
            <TextBox x:Name="TxtRemotePath" Style="{StaticResource FieldBox}"/>
            <Label Content="{l:Loc 'URL pubblico del manifest (link della condivisione, per il download)'}"/>
            <TextBox x:Name="TxtPublicUrl" Style="{StaticResource FieldBox}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{l:Loc 'Annulla'}" Width="90" Height="32" Margin="0,0,10,0" IsCancel="True"/>
            <Button Content="{l:Loc 'Salva'}" Style="{StaticResource PrimaryButton}" Width="90" Height="32" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>
